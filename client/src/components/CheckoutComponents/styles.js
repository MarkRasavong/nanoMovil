import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    headerText: {
        color: 'black',
        fontFamily: 'var(--font-header)',
    },
    listItem: {
        color: 'black',
    }
}))