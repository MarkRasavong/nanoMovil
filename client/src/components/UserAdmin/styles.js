import { makeStyles } from '@material-ui/styles';

const useStyles = makeStyles((theme) => ({
    toolbar: {
        marginBottom: '10px'
    },
    submit: {
        margin: '20px 0'
    }
}));

export default useStyles;