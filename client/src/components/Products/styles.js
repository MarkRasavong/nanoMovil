import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    headerText: {
        color: 'var(--vlcWhite)',
        fontFamily: 'var(--font-header)'
    },
    radioGroup: {
        color: 'var(--vlcWhite)',
    }
}))